spring:
  application:
    name: dubbo-springboot-provider
dubbo:
  protocol:
    name: dubbo
    port: -1

  registry:
    address: nacos://127.0.0.1:8848 # 配置中心地址
    register-mode: instance # 注册模式：instance interface all
    parameters:
      username: nacos
      password: nacos
      namespace: 57eb215b-9dfe-4e85-9543-faba95316f98
